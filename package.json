{
  "name": "solanco-pay",
  "private": true,
  "version": "0.0.1",
  "packageManager": "pnpm@10.2.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "format": "prettier -w .",
    "format:check": "prettier -c ."
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
